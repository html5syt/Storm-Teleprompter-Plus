请阅读notes\ori_code下的文件，了解飓风提词器的技术实现细节，并基于此代码库和notes\ori-prompt.md的详细技术说明，将其改写和扩充到当前所处的Flutter项目中。（你可以删除Flutter项目中原有的计数器示例代码）
要求：
1. 代码实现必须基于Flutter框架，且符合Flutter的最佳实践和项目架构规范，而不是原来的React项目架构。
2. 需要将飓风提词器的完整功能迁移到Flutter项目中，包括但不限于文本滚动、速度调节、界面设计等。
3. 代码结构要清晰，模块化设计，便于后续维护和扩展。
4. 在实现过程中，可以适当优化原有的代码结构和逻辑，但必须保证功能的一致性和稳定性。
5. 需要在Flutter项目中添加必要的注释和文档说明（包括README），帮助开发者理解代码实现细节。
6. 注意校验上下文以免应用代码patch时出现问题
7. 注意你的效率，不要长时间重复调用同一个工具（这种情况通常发生在上下文压缩之后，所以请你善用相关记忆工具维护当前改写的状态等）
8. ui界面布局参考飓风提词器的设计，并使用Flutter的Material Design组件库，确保界面美观且用户友好。
9. 原有前后端分离架构需要合并，实现单次运行即可使用的目的。
10. 不得出现缺少实现、占位代码等情况，遇到无法直接转换的代码时请你自己决策实现，保证最终的代码库是完整可运行的，并且功能与原有的飓风提词器一致或更优。
11. 你拥有在本地执行命令的权限，但是请注意除非需要调用外部程序（如安装插件等），否则请尽量避免调用命令行工具而是使用vscode提供的工具，以免影响效率和上下文的稳定性。
特别说明：
1. 去除所有与飞书API相关的代码和功能，确保项目独立于飞书平台。
2. 项目中原有的Sherpa-Onnx wasm实现改为Flutter的原生插件调用，并且支持动态选择和下载模型（下载要支持镜像加速以供国内用户使用）以供用户参考设备性能选择。你可以参考sherpa-onnx的GitHub仓库（https://github.com/k2-fsa/sherpa-onnx）中的各项示例和说明文档，了解如何在Flutter中实现原生插件调用，并且支持动态选择和下载模型的功能。
3. 确保项目可以跨平台使用
4. 要增加全屏模式以供用户在实际演讲时使用，并且支持自动隐藏界面元素以提供更好的观看体验。