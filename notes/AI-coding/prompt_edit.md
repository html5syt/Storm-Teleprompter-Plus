你现在需要结合项目flutter代码和原有ts版本的提词器源代码，对以下重要内容进行修改：
1. 当前读到的字的位置需要实时跟随位置阅读区域框中心，即使用户正在调整阅读框偏移
2. 手动模式下方向键可以移动当前读到的字的位置，移动时同样需要保持当前读到的字在阅读区域框中心；自动模式下未开始时同样支持方向键调整当前读到的字的位置，开始后则不响应左右键，而是上下键调整滚动速度
3. 方向键、鼠标滚轮、屏幕手势实现的功能应该相同
4. 在优化界面流畅度的同时尽量采用原有滚动和对齐底层算法，并且使在点击任意一个字时能准确地将该字作为当前阅读位置并对齐到阅读区域框中心。注意：该点十分重要，请务必保证实现的准确性和稳定性，否则其他功能实现可能会出现一些由该问题引发的附加问题
5. “滚动到底部”和“重置到开头”进行整合，当在稿件不到50%的地方时时点击一次滚动到底部（视为读到最后一个字），反之则重置到开头
6. 富文本编辑器借助插件实现“所见即所得”功能，并且文字背景颜色在编辑器和提词器界面不能有偏差（比如标准红色变为深红色）
7. 使用第三方插件实现可靠、完整、统一的颜色选择器功能
8. 稿件管理新增文件夹功能，类似资源管理器的文件夹功能，允许用户创建、重命名、删除文件夹，并且支持将稿件拖放到不同的文件夹中进行分类管理
9. 编辑器应支持实时保存，去除手动保存按钮
10. 编辑器增加快速开始提词的按钮，点击后将当前编辑的内容保存为新稿件并直接进入提词模式预览
11. 提词器的字体大小、行高、字间距、水平边距、阅读区域框的位置参数默认应该根据稿件和屏幕大小自动调整，允许用户手动调整（将这些选项默认收纳起来），手动调整后则不再自动调整，除非用户选择重置自动调整。参考原有的自动调整算法
12. 界面字体、提词器正文字体选取器应该支持选择系统上所有可用字体，而不是预设的几个字体选项
13. 默认情况下：在手动模式时关闭已读字加灰与高亮当前字的功能，在自动模式时开启已读字加灰功能，关闭高亮当前字的功能。如果用户进行了调整则以用户的设置为准
14. 新增：Esc键快速退出提词器界面，而不是退出全屏
15. 注意全平台的响应式界面适配和输入事件处理，确保在不同平台（Windows、macOS、Linux、Web）上都能获得一致的用户体验
16. 检查代码整体框架并进行整理使其更清晰、模块化，便于后续维护和扩展且有效辅助以上问题的修复和添加
注意：
1. 无论之前是否进行过类似的修改，都需要检查并确保以上功能在当前代码库中得到实现，并且功能完整、稳定。
2. 在实现过程中，注意保持代码结构清晰、模块化设计，便于后续维护和扩展。
3. 不要略过任何一个要点，确保每个要点都得到实现。
4. 完成每一点后明确表明已完成，且正确使用vscode提供的todo工具管理进度。全部完成后简要报告所有修改。